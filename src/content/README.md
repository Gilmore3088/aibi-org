# Assessment Content

This is a redirect marker. The assessment content lives at the repo root in
`content/assessments/v1/` — not here. Keep this directory empty so we always
import from `@/../content/assessments/v1` (or however tsconfig path resolves it).

If content moves into `src/`, update CLAUDE.md and tsconfig `paths` at the same time.
