// Re-export from the lesson surface so the toolbox folder has a stable
// entry point if other surfaces want to render the same save flow.
export { SaveAsArtifactButton } from '@/components/addie/lesson/SaveAsArtifactButton';
