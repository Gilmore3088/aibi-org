---
phase: 08-certificate-verification
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/app/verify/[certificateId]/page.tsx
autonomous: false
requirements: [CERT-05]

must_haves:
  truths:
    - "Navigating to /verify/[certificateId] without logging in returns holder name, designation, date issued, and issuing institution"
    - "No additional personal data is exposed (no email, no scores, no enrollment details)"
    - "Invalid certificate ID shows a clear 'not found' message"
  artifacts:
    - path: "src/app/verify/[certificateId]/page.tsx"
      provides: "Public verification page for certificate authenticity"
      min_lines: 40
  key_links:
    - from: "src/app/verify/[certificateId]/page.tsx"
      to: "certificates table"
      via: "Supabase anon client query by certificate_id"
      pattern: "from\\('certificates'\\).*eq.*certificate_id"
---

<objective>
Public certificate verification endpoint at /verify/[certificateId] that returns holder name, designation, date issued, and issuing institution — nothing else. No authentication required. This is the URL printed on every certificate PDF.

Purpose: Third parties (employers, regulators, LinkedIn viewers) can verify a credential is authentic by visiting this URL.
Output: A single public page component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-certificate-verification/08-CONTEXT.md
@.planning/phases/08-certificate-verification/08-01-SUMMARY.md

@src/types/course.ts
@supabase/migrations/00001_course_tables.sql

<interfaces>
<!-- The certificates table is publicly readable (RLS: USING (true) for anon, authenticated) -->

From supabase/migrations/00001_course_tables.sql:
```sql
CREATE TABLE IF NOT EXISTS certificates (
  id             uuid        PRIMARY KEY DEFAULT gen_random_uuid(),
  enrollment_id  uuid        NOT NULL REFERENCES course_enrollments(id) ON DELETE CASCADE,
  certificate_id text        NOT NULL UNIQUE,
  holder_name    text        NOT NULL,
  designation    text        NOT NULL DEFAULT 'AiBI-P',
  issued_at      timestamptz NOT NULL DEFAULT now(),
  UNIQUE (enrollment_id)
);

CREATE POLICY "Public read certificates" ON certificates
  FOR SELECT TO anon, authenticated
  USING (true);
```

From src/types/course.ts:
```typescript
export interface Certificate {
  readonly id: string;
  readonly enrollment_id: string;
  readonly certificate_id: string;
  readonly holder_name: string;
  readonly designation: string;
  readonly issued_at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public verification page at /verify/[certificateId]</name>
  <files>src/app/verify/[certificateId]/page.tsx</files>
  <action>
Create a server component page at `src/app/verify/[certificateId]/page.tsx`. This is a PUBLIC page — no auth required, no course layout shell, no sidebar.

**Data fetching:**
- Since this is a server component that needs to work without auth, create a Supabase client using the anon key directly. The RLS policy allows anon SELECT on certificates.
- Import `createClient` from `@supabase/supabase-js` and create an anonymous client using `process.env.NEXT_PUBLIC_SUPABASE_URL` and `process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY`
- Query: `from('certificates').select('holder_name, designation, issued_at').eq('certificate_id', params.certificateId).single()`
- CRITICAL per CERT-05: SELECT only `holder_name`, `designation`, `issued_at` — do NOT select `id`, `enrollment_id`, or any other column. The issuing institution is always "The AI Banking Institute" (hardcoded, not from DB).

**Not found state:**
- If no certificate matches, render a clean page with: "Certificate Not Found" heading, "The certificate ID you entered could not be verified. Please check the ID and try again." message, a link back to aibankinginstitute.com.

**Found state — display exactly four items per CERT-05:**
1. **Holder Name** — displayed prominently (Cormorant-style large text)
2. **Designation** — "AiBI-P -- Banking AI Practitioner" (or whatever designation value is in the record)
3. **Date Issued** — formatted as "Month DD, YYYY" (use Intl.DateTimeFormat)
4. **Issuing Institution** — "The AI Banking Institute" (hardcoded)

**Layout:**
- Clean, standalone page — NOT inside the course layout
- Parchment background (bg-linen or bg-parch from Tailwind config, or use inline style with #f9f6f0)
- Centered card with the AiBI brand mark at top
- A green checkmark or verified indicator (use a simple SVG checkmark, not an icon library)
- "Certificate Verified" heading in terra color
- The four data fields displayed cleanly with labels
- Footer: "The AI Banking Institute -- AIBankingInstitute.com"
- No links to course content, no login prompts, no upsells — this is a trust page

**SEO / metadata:**
- Export a `generateMetadata` function: title "Certificate Verification -- The AI Banking Institute", description "Verify the authenticity of an AI Banking Institute credential."
- Add `robots: 'noindex'` — verification pages should not be indexed (they contain personal names)

**Accessibility:**
- All text meets 4.5:1 contrast ratio
- Semantic HTML: use heading hierarchy (h1 for "Certificate Verified", h2 for holder name)
- The page should be completely functional without JavaScript (server-rendered)
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Projects/TheAiBankingInstitute && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- /verify/AIBIP-2026-XXXXXX renders holder name, designation, date, institution with no auth required
- Invalid certificate ID renders "Certificate Not Found" message
- Only holder_name, designation, issued_at are queried from DB — no PII leakage
- Page is a standalone server component with no course layout shell
- noindex meta tag prevents search engine indexing of personal names
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full certificate pipeline end-to-end</name>
  <files>src/app/verify/[certificateId]/page.tsx</files>
  <action>
Human verification of the complete certificate issuance pipeline built across Plans 01 and 02. Reviewer approval triggers certificate record + PDF generation, learner downloads from /courses/aibi-p/certificate, public verification at /verify/[certificateId].
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Projects/TheAiBankingInstitute && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
Human has verified: (1) reviewer approval creates certificate record, (2) PDF downloads with correct typography, (3) /verify page shows only 4 fields without auth, (4) invalid IDs show not-found, (5) no duplicate certificates on re-approval.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> /verify/[certificateId] | Unauthenticated access to certificate verification data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information Disclosure | /verify/[certificateId] | mitigate | SELECT only holder_name, designation, issued_at — never enrollment_id, email, or scores |
| T-08-08 | Denial of Service | /verify/[certificateId] | accept | Simple SELECT query on indexed column; Next.js ISR/caching can be added if needed; low-value target |
| T-08-09 | Spoofing | certificate ID enumeration | accept | Certificate IDs are 6-char alphanumeric from 30-char alphabet (~730M combinations); sequential enumeration impractical; noindex prevents search indexing |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. /verify/{validCertId} shows exactly 4 fields: holder name, designation, date, institution
4. /verify/{invalidId} shows "Certificate Not Found"
5. Page source confirms no enrollment_id, email, or score data in HTML
6. Human verification of full end-to-end flow (checkpoint task)
</verification>

<success_criteria>
- /verify/[certificateId] returns holder name, designation, date, institution without authentication
- No personal data beyond holder name is exposed
- Invalid IDs show a clear not-found state
- Page is noindex to prevent name indexing by search engines
- Full pipeline verified end-to-end: approval -> certificate record -> PDF download -> public verification
</success_criteria>

<output>
After completion, create `.planning/phases/08-certificate-verification/08-02-SUMMARY.md`
</output>
