---
phase: 02-course-shell-assessment-upgrade
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/courses/aibi-p/layout.tsx
  - src/app/courses/aibi-p/_components/CourseSidebar.tsx
  - src/app/courses/aibi-p/_components/MobileSidebarDrawer.tsx
  - src/app/courses/aibi-p/_components/ProgressIndicator.tsx
  - src/app/courses/aibi-p/page.tsx
  - src/app/courses/aibi-p/_components/PillarCard.tsx
  - src/app/courses/aibi-p/_components/ModuleMapItem.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04, SHELL-08, SHELL-09, SHELL-10]

must_haves:
  truths:
    - "Visitor sees /courses/aibi-p with four pillar cards (Awareness/Sage, Understanding/Cobalt, Creation/Amber, Application/Terra)"
    - "Visitor sees 9-module map listing module numbers, titles, pillar color, estimated time, and key output"
    - "Persistent left sidebar shows modules grouped by pillar with current=highlighted, completed=check, locked=greyed+lock"
    - "Sidebar collapses to hamburger drawer on mobile (below lg breakpoint)"
    - "Progress indicator shows module-level completion across 9 steps"
    - "Page is fully responsive at 390px width without horizontal scrolling"
  artifacts:
    - path: "src/app/globals.css"
      provides: "--color-amber CSS variable"
      contains: "--color-amber"
    - path: "src/app/courses/aibi-p/layout.tsx"
      provides: "Shared layout wrapping sidebar + main content area"
      exports: ["default"]
    - path: "src/app/courses/aibi-p/page.tsx"
      provides: "Course overview page with pillar cards and module map"
      exports: ["default"]
    - path: "src/app/courses/aibi-p/_components/CourseSidebar.tsx"
      provides: "Desktop sidebar component with pillar-grouped modules"
      exports: ["CourseSidebar"]
  key_links:
    - from: "src/app/courses/aibi-p/layout.tsx"
      to: "CourseSidebar"
      via: "import and render in layout"
      pattern: "import.*CourseSidebar"
    - from: "src/app/courses/aibi-p/page.tsx"
      to: "@content/courses/aibi-p"
      via: "import modules array"
      pattern: "import.*modules.*from.*@content/courses/aibi-p"
---

<objective>
Build the course shell layout (shared sidebar + main content area) and the course overview page at `/courses/aibi-p`. The sidebar groups modules by pillar with visual states (completed/current/locked). The overview page shows four pillar cards with pillar-specific colors and a 9-module map.

Purpose: This is the navigational skeleton that all module pages will render inside. Without the layout and sidebar, no module page can exist.
Output: Working `/courses/aibi-p` route with sidebar navigation, pillar cards, and module map -- all responsive to 390px.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key types the executor needs from Phase 1 content layer -->

From content/courses/aibi-p/types.ts:
```typescript
export type Pillar = 'awareness' | 'understanding' | 'creation' | 'application';

export const PILLAR_META: Record<Pillar, { readonly label: string; readonly colorVar: string }> = {
  awareness:     { label: 'Awareness',     colorVar: 'var(--color-sage)' },
  understanding: { label: 'Understanding', colorVar: 'var(--color-cobalt)' },
  creation:      { label: 'Creation',      colorVar: 'var(--color-terra-light)' },
  application:   { label: 'Application',   colorVar: 'var(--color-terra)' },
};

export interface Module {
  readonly number: number;
  readonly id: string;
  readonly title: string;
  readonly pillar: Pillar;
  readonly estimatedMinutes: number;
  readonly keyOutput: string;
  readonly sections: readonly Section[];
  readonly tables?: readonly ContentTable[];
  readonly activities: readonly Activity[];
  readonly artifacts?: readonly ArtifactDefinition[];
  readonly mockupRef: string;
  readonly roleSpecific?: boolean;
}
```

From content/courses/aibi-p/modules.ts:
```typescript
export const modules: readonly Module[] = [module1, ..., module9];
export function getModuleByNumber(n: number): Module | undefined;
```

From src/types/course.ts:
```typescript
export interface CourseEnrollment {
  readonly completed_modules: readonly number[];
  readonly current_module: number;
}
```
</interfaces>

<design_reference>
HTML mockup source of truth: content/courses/AiBI-P v1/stitch_ai_banking_institute_course/aibi_p_refined_course_overview/code.html

Key design patterns from mockup:
- Fixed left sidebar (w-72, hidden below lg, bg-linen, border-r border-terra/10)
- Main content offset with lg:ml-72
- Pillar cards in 4-column grid on lg, 2-col on md, 1-col on mobile
- Module map items: module number (DM Mono, terra), title (serif font), progress bar, status text
- Locked modules have opacity-40
- Sidebar has: logo area, pillar section links with icons, Resume button at bottom
- Mobile: bottom nav bar (deferred -- use hamburger drawer instead per CONTEXT.md decision)
- Typography: Cormorant for headings (mapped to font-headline in Tailwind), DM Sans for body, DM Mono for numbers
- Color discipline: sage=Awareness only, cobalt=Understanding only, amber=Creation only, terra=Application+brand

IMPORTANT: The mockup uses "Newsreader" and "Work Sans" fonts. The project uses Cormorant and DM Sans per CLAUDE.md designer brief. Map accordingly:
- Newsreader/serif headings -> font-[family-name:Cormorant] or the project's serif class
- Work Sans body -> font-[family-name:DM_Sans] or the project's body font class
- DM Mono stays as-is
</design_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --color-amber CSS variable, create course layout with persistent sidebar</name>
  <files>src/app/globals.css, src/app/courses/aibi-p/layout.tsx, src/app/courses/aibi-p/_components/CourseSidebar.tsx, src/app/courses/aibi-p/_components/MobileSidebarDrawer.tsx, src/app/courses/aibi-p/_components/ProgressIndicator.tsx</files>
  <action>
1. **globals.css**: Add `--color-amber: #c48820;` (warm amber for Creation pillar -- distinct from terra-light) in the `:root` block alongside the existing color variables. Also add `--color-amber-light: #d4a032;` for hover states.

2. **PILLAR_META update**: Update `content/courses/aibi-p/types.ts` to change `creation` colorVar from `'var(--color-terra-light)'` to `'var(--color-amber)'`. This is critical -- CONTEXT.md decision says amber for Creation pillar.

3. **CourseSidebar.tsx**: Server Component (no 'use client'). Props: `{ completedModules: readonly number[], currentModule: number }`.
   - Import `modules` from `@content/courses/aibi-p` and `PILLAR_META`.
   - Group modules by pillar using a Map. Pillar order: awareness, understanding, creation, application.
   - Each pillar group: heading with pillar label in pillar color, then module links.
   - Module link states:
     - Completed: check icon (Unicode checkmark or Lucide icon if available), normal opacity, links to `/courses/aibi-p/${module.number}`
     - Current: highlighted background (bg-[color:var(--color-parch-dark)]), bold text, links to module
     - Locked: greyed out (opacity-40), lock icon, NOT an anchor tag (use span/div -- not clickable)
   - At bottom: "Resume" button linking to `/courses/aibi-p/${currentModule}`
   - Style matching mockup: w-72, bg-[color:var(--color-linen)], border-r, pt-20 for header offset
   - Fixed positioning: `fixed left-0 top-0 h-full`
   - Hidden below lg: `hidden lg:flex flex-col`

4. **MobileSidebarDrawer.tsx**: Client Component ('use client'). Props: same as CourseSidebar.
   - Hamburger button (three horizontal lines icon) positioned in a sticky header bar visible only below lg breakpoint
   - On click: slide-out drawer from left with same sidebar content
   - Overlay backdrop on open, close on backdrop click or X button
   - Use `useState` for open/close, `transition-transform` for slide animation
   - `aria-label="Open course navigation"`, `aria-expanded`, focus trap not required for Phase 2 but add `role="dialog"`

5. **ProgressIndicator.tsx**: Server Component. Props: `{ completedModules: readonly number[], totalModules: number }`.
   - Renders 9 small circles/dots in a horizontal row
   - Completed = filled with terra, incomplete = outline only
   - Below the dots: "N of 9 modules complete" text in DM Mono
   - Compact enough to fit in sidebar or overview page

6. **layout.tsx**: Server Component at `src/app/courses/aibi-p/layout.tsx`.
   - Renders CourseSidebar on left (desktop), MobileSidebarDrawer (mobile)
   - Main content area with `lg:ml-72 pt-16 min-h-screen`
   - For now, hardcode `completedModules: []` and `currentModule: 1` -- these will be wired to Supabase in Plan 03. Add a `// TODO: Read from enrollment (Plan 02-03)` comment.
   - Export as default layout (App Router convention)
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Projects/TheAiBankingInstitute && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
  - globals.css contains --color-amber variable
  - PILLAR_META.creation.colorVar is 'var(--color-amber)'
  - CourseSidebar renders 9 modules grouped by 4 pillars with correct visual states
  - MobileSidebarDrawer opens/closes on mobile with hamburger trigger
  - layout.tsx wraps children with sidebar on left + main content area
  - All files compile with zero TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build course overview page with pillar cards and module map</name>
  <files>src/app/courses/aibi-p/page.tsx, src/app/courses/aibi-p/_components/PillarCard.tsx, src/app/courses/aibi-p/_components/ModuleMapItem.tsx</files>
  <action>
1. **PillarCard.tsx**: Server Component. Props: `{ pillar: Pillar, label: string, colorVar: string, description: string, moduleCount: number, status: 'completed' | 'in-progress' | 'locked' }`.
   - Card with parchment background (bg-[color:var(--color-parch)])
   - Icon area with pillar color border (seal-border pattern from mockup)
   - Pillar name in Cormorant serif
   - Description text
   - Status badge: "Completed" in pillar color, "In Progress" in pillar color bold, "Locked" in dust color
   - Hover: bg-[color:var(--color-parch-dark)] transition
   - Per SHELL-01: four pillars with Sage, Cobalt, Amber, Terra colors

2. **ModuleMapItem.tsx**: Server Component. Props: `{ module: Module, status: 'completed' | 'current' | 'locked', completionTime?: string }`.
   - Module number in DM Mono, pillar color
   - Module title in serif font
   - Progress bar: full width colored for completed, partial for current, empty outline for locked
   - Status text: "Completed * Nm" / "In Progress" / "Locked"
   - Locked modules: opacity-40 matching mockup pattern
   - Per SHELL-02: shows number, title, pillar assignment (via color), estimated time, key output

3. **page.tsx** (course overview): Server Component at `src/app/courses/aibi-p/page.tsx`.
   - Import `modules` and `PILLAR_META` from `@content/courses/aibi-p`
   - Hero section: "Banking AI Practitioner" title in Cormorant, subtitle, "Resume Course" and "View Syllabus" buttons
   - Four Pillars section: 4-column grid (lg), 2-col (md), 1-col (mobile) of PillarCard components
     - Pillar descriptions derived from mockup: Awareness="Identifying AI opportunities...", Understanding="Platform mastery...", Creation="Building skills...", Application="Real-world automation..."
     - For now, all pillars show 'locked' status except awareness shows 'in-progress' (hardcoded -- wired to enrollment in Plan 03)
   - Module Map section: 3-column grid (lg), 2-col (md), 1-col (mobile) of ModuleMapItem components
     - Iterate over `modules` array, render each with status
     - For now, hardcode module 1 as 'current', rest as 'locked'
   - ProgressIndicator component showing completion state
   - Style per mockup: max-w-6xl mx-auto px-8 py-16, section headings in Cormorant with terra accent
   - Add metadata export: `export const metadata = { title: 'AiBI-P: Banking AI Practitioner | The AI Banking Institute' }`
   - SHELL-09: Ensure all grid layouts collapse properly on 390px (single column, no overflow)
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Projects/TheAiBankingInstitute && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - /courses/aibi-p renders with four pillar cards showing correct pillar colors (Sage, Cobalt, Amber, Terra)
  - 9-module map displays all modules with numbers, titles, estimated times, key outputs
  - Page renders without errors and builds successfully
  - Layout is responsive: single column at 390px, multi-column at desktop
  - Metadata title is set correctly
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No trust boundaries | This plan creates static UI components only; no user input, no API calls, no auth checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Course content visible to non-enrolled users | accept | SHELL-12 enrollment gating addressed in Plan 03; overview page is intentionally public (marketing funnel) |
| T-02-02 | Tampering | Hardcoded progress state | accept | Temporary hardcoded values replaced with Supabase reads in Plan 03 |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully
3. Dev server: navigating to `/courses/aibi-p` renders the overview with sidebar
4. Sidebar shows 4 pillar groups with 9 modules
5. On mobile viewport (390px), sidebar is hidden and hamburger button appears
</verification>

<success_criteria>
- Course overview page renders at /courses/aibi-p with four pillar cards and 9-module map
- Sidebar component renders module list grouped by pillar with visual states
- Mobile sidebar drawer opens/closes via hamburger icon
- --color-amber CSS variable exists and PILLAR_META.creation uses it
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-course-shell-assessment-upgrade/02-01-SUMMARY.md`
</output>
