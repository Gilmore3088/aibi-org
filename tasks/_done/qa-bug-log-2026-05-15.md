# QA bug log — issues #92 → #96

Running tally of bugs found while iterating through the five-issue QA
sweep. Each entry: where it was caught, what failed, what was changed,
and the commit that landed the fix. Anything still open at the bottom.

---

## Issue #92 — homepage ladder + funnel QA

### Bug 92-1 — pre-email score CTA hardcoded to Foundation for lower tiers
**Found:** Playwright drove the free 12-question assessment to a `starting-point` score; the big primary CTA on the score-reveal screen read "Explore the Foundation course" and routed to `/courses/foundation/program`.

**Root cause:** `src/app/assessment/page.tsx` line 94-100. `isLowerTier` branch hardcoded a Foundation CTA. `NextStepCards` correctly routes lower tiers to In-Depth as primary, but `NextStepCards` only renders POST email capture, so the pre-email funnel never reached it.

**Per issue #88/#92:** lower tiers (`starting-point`, `early-stage`) should land on the In-Depth Assessment ($99) as the deeper diagnostic before the $295 course commitment.

**Fix (commit `5792e17`):** swap the lower-tier pre-email CTA to "Take the In-Depth Assessment · $99" → `/assessment/in-depth`. Higher tiers continue to Executive Briefing.

**Re-verified:**
- starting-point → "Take the In-Depth Assessment · $99" ✓
- early-stage → "Take the In-Depth Assessment · $99" ✓
- building-momentum → "Book Your Executive Briefing" ✓
- ready-to-scale → "Book Your Executive Briefing" ✓


### Bug 92-2 — /assessment had no H1
**Found:** Axe-style a11y pass on `/assessment` returned headings `H2,H3` only. No H1 means screen-reader users have no top-level page anchor.

**Fix:** add `<h1 className="sr-only">AI Readiness Assessment</h1>` at the top of the `<main>` in `src/app/assessment/page.tsx`. Visually hidden so layout doesn't shift; semantically present.

**Re-verified:** `/assessment` now reports `H1,H2,H3`.

### Bug 92-3 — Foundation pages skipped heading levels (H1 → H3)
**Found:** `/courses/foundation/program` and `/courses/foundation/program/purchase` rendered `H1, H3, H2, H3` — H3 appearing before H2 violates WCAG 1.3.1.

**Root cause:** In both pages the "current module name" / "$295 price" block used `<h3>` as a visual style, not a semantic level.

**Fix:** bump `<h3>` → `<h2>` for the current-module title and the price block. Same visual; correct hierarchy.

**Re-verified:** both pages now report `H1, H2, H2, H3`.

### Bug 92-4 — post-email closing CTA also hardcoded to Foundation (DEAD CODE found)
**Found:** After fixing the pre-email score CTA (Bug 92-1), drove the email gate to land on the actual results screen. The post-email closing CTA still read "Enroll your team in AiBI-Foundation" for `starting-point` and `early-stage`, contradicting the same ladder rule.

**Critical root cause:** **`NextStepCards.tsx` is dead code.** The post-email view is `ResultsViewV2.tsx`, which imports `TIER_CLOSING_CTA` from `content/assessments/v2/personalization.ts`. My earlier "fix" of NextStepCards (Bug 92-1 partial) had zero user-facing impact because that component is never imported by the live results view.

**Fix:** updated `TIER_CLOSING_CTA['starting-point']` and `TIER_CLOSING_CTA['early-stage']` to:
- ctaLabel: "Take the In-Depth Assessment · $99"
- ctaHref: "/assessment/in-depth"
- Headline + body rewritten to the "go deeper before you commit" framing matching the homepage.

**Re-verified live (Playwright, all four tiers):**
- starting-point → `Take the In-Depth Assessment · $99` → `/assessment/in-depth` ✓
- early-stage → `Take the In-Depth Assessment · $99` → `/assessment/in-depth` ✓
- building-momentum → `Request an Executive Briefing` → `/for-institutions/advisory` ✓
- ready-to-scale → `Request a conversation` → `/for-institutions/advisory` ✓

**Open follow-up:** `NextStepCards.tsx` is unused. Either re-wire (if intentional) or delete. Logged for #93 follow-up.

### Bug 92-5 — `/robots.txt` and `/sitemap.xml` get rewritten to coming-soon HTML
**Found:** Playwright smoke suite failed `robots.txt disallows /api, /auth, /dashboard`. Fetched the route manually — it returned `text/html` with the "We will be helping you with all the AI noise soon" coming-soon body instead of the robots.txt rules.

**Root cause:** `src/middleware.ts` only bypassed prefix paths (`/api`, `/assessment`, …) under `COMING_SOON=true`. `/robots.txt` and `/sitemap.xml` aren't in any prefix and got rewritten. Same issue applies to the favicon SVGs.

**Why this matters in production:** if the site goes into coming-soon mode again (e.g., during a maintenance window), search-engine crawlers will see HTML at `/robots.txt` and `/sitemap.xml` instead of the actual SEO rules — degrades indexing for the post-takedown launch.

**Fix:** added `COMING_SOON_BYPASS_EXACT` list with `/robots.txt`, `/sitemap.xml`, `/icon.svg`, `/apple-icon.svg`. Middleware bypass logic checks exact-match list before the prefix list.

**Re-verified:** `/robots.txt` now serves plain-text rules, `/sitemap.xml` serves XML.

### Bug 92-6 — homepage tagline e2e test fails on strict-mode (two valid matches)
**Found:** smoke test `homepage renders with tagline` failed because `getByText(/Turning Bankers into Builders/i)` matched both the hero H1 and the SiteFooter wordmark subtitle (two legitimate places — brand reinforcement).

**Fix:** scope the assertion to the hero H1: `page.getByRole('heading', { level: 1, name: /Turning Bankers into Builders/i })`. Test-side change only.

### Bug 92-7 — 404 e2e test fails under COMING_SOON takedown
**Found:** smoke test `404 page renders for unknown route` failed with `status=200` because unknown routes get rewritten to `/coming-soon` while takedown is active (correct production behavior).

**Fix:** detect the coming-soon body in the response and `test.skip(true, …)` so the assertion only fires when the real site is live. Test-side change only.

## #92 — pre-existing items (out of scope, logged for follow-up)
These were surfaced by the e2e + axe scans but predate issue #88's work and aren't blockers for #92's acceptance criteria. Bundled here so they aren't lost.

- **a11y/`/education`:** `<div role="list">` contains `<article>` children, not `role="listitem"` → `aria-required-children` (critical).
- **a11y/color-contrast:** `text-dust` "INSTITUTE" wordmark subtitle on linen fails WCAG AA contrast at small sizes; multiple locations.
- **a11y/Foundation purchase:** `aria-prohibited-attr` on decorative dot spans in the LMS sidebar.
- **e2e/auth:** ~9 auth tests fail because no Supabase test users exist locally; needs seeded fixtures.
- **e2e/api-gates:** 5 API endpoints don't reject unauthenticated requests as expected; need rate-limiting / auth gate middleware.
- **e2e/marketing-extended:** `/security` page download CTA missing, meta description missing on some pages, `/.well-known/security.txt` missing.

## #92 — verdict
**PASS** for acceptance criteria. Six real bugs found and fixed (four product-flow, two middleware/SEO). Two test-side brittleness fixes. Pre-existing a11y + auth + API-gate failures logged for separate tickets.

---

## Issue #93 — In-Depth Assessment hardening

### Bug 93-1 — `/assessment` meta description claimed "Eight questions"
**Found:** `src/app/assessment/layout.tsx` metadata description: "Eight questions. Under three minutes…" The free assessment is 12 questions, not 8.

**Fix:** updated to "Twelve questions. Under three minutes…"

### Bug 93-2 — `/assessment/in-depth/take` meta description said "four readiness dimensions"
**Found:** the take page (where users land *after* purchase) listed "Forty-eight questions across the four readiness dimensions" in `metadata.description`. The actual assessment uses eight dimensions.

**Fix:** rewrote to "Forty-eight questions across the eight readiness dimensions. Returns a personalized Briefing with peer-band comparison, a deep-dive on your lowest-scoring dimensions, and an action register scaffold."

### Bug 93-3 — purchased-page HIGHLIGHTS overpromised
**Found:** `/assessment/in-depth/purchased` HIGHLIGHTS listed:
- "20-page personalized report" — actual Briefing TOC ends at p. 07 (~10–12 pages total)
- "30-day action plan" — actual Briefing action register is a 90-day window

**Fix:** rewrote highlights to match reality:
- "Personalized Briefing with peer-band comparison and dimension deep-dives"
- "90-day action register keyed to your lowest-scoring dimensions"

### Bug 93-4 — silent redirect from `/take` to `/in-depth` for non-buyers
**Found:** non-purchasers attempting `/assessment/in-depth/take` get `redirect('/assessment/in-depth?reason=no-purchase')`, but the landing page ignored the `?reason` query param. User lands on the marketing page with no explanation of why they were bounced.

**Fix:** the In-Depth landing page now reads `searchParams.reason` and renders a "Purchase required" status notice above the pricing section when `reason=no-purchase`. Tells the user to buy a seat, or sign in with the email they used at checkout.

**Re-verified:** `/assessment/in-depth?reason=no-purchase` now returns HTML containing the "Purchase required" notice.

### Verified by code-reading (cannot drive Stripe live in this env)
- Stripe Checkout session uses `STRIPE_INDEPTH_PRICE_ID` with `metadata.product='in-depth-assessment'` (`src/app/api/checkout/in-depth/route.ts`).
- Successful purchase redirects to `/assessment/in-depth/purchased` (success_url).
- Webhook handler in `provision-enrollment.ts` reads `product='in-depth-assessment'` and inserts `course_enrollments` row with that product slug.
- `/take` gates on Supabase auth + `course_enrollments.product='in-depth-assessment'`. Gmail-alias entitlement match via `emailVariants(user.email)` already wired.
- Non-buyer attempting `/take` gets `redirect('/assessment/in-depth?reason=no-purchase')`. Logged-out user gets `redirect('/auth/login?next=/assessment/in-depth/take')`.
- Dashboard In-Depth card only renders when `assessments.inDepth.entitled` is true. Completed users see "View your Briefing" link; un-completed see "Take your purchased assessment."
- Team checkout: API returns 503 "Email hello@aibankinginstitute.com for bulk orders" if mode=institution requested. PurchaseButton only emits mode=individual, so this is gated correctly. Public copy says "Want your whole team to benefit? Contact us for ten or more." — consistent with the checklist.

### #93 — environmental limitation
Local dev has `.env.local` wiped (only `COMING_SOON` present per CLAUDE.md 2026-05-10 memory). Cannot drive Stripe Checkout end-to-end here. Code paths verified by reading; full integration test belongs in staging once env vars are restored.

### Verdict
**PASS.** Four real copy / UX bugs found and fixed. Functional flow verified by code-reading; live Stripe integration validation deferred to staging (env limitation).

---

## Issue #94 — Foundation checkout + lifetime access entitlement proof

### Bug 94-1 — `/artifacts/[artifactId]` not entitlement-gated (LEAK)
**Found:** `src/app/courses/foundation/program/artifacts/[artifactId]/page.tsx` rendered the full artifact detail with no enrollment check. Anyone could view any artifact template via direct URL — defeats the "lifetime access to artifact templates" lifetime promise.

**Fix:** added `getEnrollment()` check at the top of the page, `redirect('/courses/foundation/program/purchase')` for non-buyers. Also added `dynamic = 'force-dynamic'` so the gate fires per request (the page had `generateStaticParams`).

### Bug 94-2 — `/tool-guides` not entitlement-gated (LEAK)
**Found:** `src/app/courses/foundation/program/tool-guides/page.tsx` rendered NotebookLM/Perplexity deep-dive guides to anyone. Same lifetime-promise violation.

**Fix:** added the same `getEnrollment()` + redirect pattern.

### Bug 94-3 — `/gallery` not entitlement-gated (LEAK)
**Found:** `src/app/courses/foundation/program/gallery/page.tsx` rendered the Output Gallery (institutional-grade AI output examples by role) to anyone.

**Fix:** added enrollment redirect.

### Bug 94-4 — `/quick-wins` not entitlement-gated (LEAK)
**Found:** `src/app/courses/foundation/program/quick-wins/page.tsx` rendered the Quick Win Tracker (logging form + recommendation-letter unlock) to anyone.

**Fix:** added enrollment redirect.

### Verified by code-reading
- **Checkout flow** — `EnrollButton` shows "Sign in to enroll" for logged-out users, links to `/auth/login?next=/courses/foundation/program/purchase`. Both `/auth/login` and `/auth/signup` honor `?next=` via `sanitizeNext()` and forward it across signup↔login.
- **Stripe session** — `/api/create-checkout` writes `metadata.product='foundation'` (canonical), uses `STRIPE_FOUNDATION_PRICE_ID` with `STRIPE_AIBIP_PRICE_ID` fallback (rename + legacy fallback intact). Success URL `/courses/foundation/program/purchased`. Cancel URL `/courses/foundation/program/purchase`.
- **Webhook + provisioning** — `/api/webhooks/stripe` uses `normalizeProduct()` to accept both `'foundation'` (new) and `'aibi-p'` (legacy retries from 2026-Q1). `provision-enrollment.ts` writes `course_enrollments` row with the canonical product slug. Webhook signature verified server-side.
- **Entitlement gates** — after fixes above, every Foundation-program asset path redirects non-enrollees:
  - `/[module]` ✓
  - `/onboarding`, `/submit`, `/toolkit`, `/settings`, `/post-assessment` ✓ (already gated)
  - `/artifacts/[artifactId]` ✓ (newly gated)
  - `/tool-guides` ✓ (newly gated)
  - `/gallery` ✓ (newly gated)
  - `/quick-wins` ✓ (newly gated)
  - `/certificate` ✓ (already gated)
  - `/prompt-library` redirects to `/dashboard/toolbox/library` which gates via `getPaidToolboxAccess()` → renders `<Paywall>` for non-buyers.
- **Dashboard card** — shows for enrolled users only; copy mentions "ninety-day action register" and "eight dimensions" (consistent with #93 fixes).

### Operator follow-up (dashboard-only, captured in deploy checklist)
Already logged in `tasks/aibi-p-to-foundation-deploy-checklist.md`:
- Stripe product display name must read **AiBI-Foundation Course** (not "AiBI-Practitioner", not "AiBI-P").
- Stripe product display name must read **In-Depth Assessment**.
- Foundation product description must mention lifetime access.

### Verdict
**PASS.** Four entitlement leaks found and gated. Checkout / webhook / provisioning flow correct by code-reading. Operator must verify Stripe dashboard display names before launch.

---

## Issue #95 — Dashboard entitlement states

### Bug 95-1 — `/practice/[repId]` not entitlement-gated (LEAK)
**Found:** Top-level `src/app/practice/[repId]/page.tsx` rendered practice rep content with no enrollment check. Same lifetime-promise violation pattern as the four leaks in #94, but on a different top-level route.

**Fix:** added `getEnrollment()` + redirect to `/courses/foundation/program/purchase`. Added `dynamic = 'force-dynamic'` so the gate fires per request despite `generateStaticParams`.

### Bug 95-2 — Dashboard "Your next step" skipped In-Depth Assessment
**Found:** Free-assessment lead (has `user.readiness`, no enrollment) saw a hardcoded "Enroll in AiBI-Foundation — $295" CTA as their primary Next Action. Same ladder-skip pattern as #92-4 (post-email closing CTA): free users were being asked to commit to the $295 course without the $99 In-Depth Assessment step.

**Fix:** rewrote the "Your next step" branch into two sub-branches:
1. **Free lead** (readiness, no enrollment, no In-Depth) → primary "Take In-Depth — $99" → `/assessment/in-depth`; secondary "Or preview Foundation"
2. **In-Depth buyer** (readiness, no enrollment, has In-Depth entitlement) → primary "Enroll in AiBI-Foundation — $295" with "You have the diagnosis. The Foundation course turns the playbook into skills…" framing

This mirrors the post-fix homepage ladder and the post-email `TIER_CLOSING_CTA` already aligned in #92-4. The dedicated In-Depth card later in the dashboard still surfaces the user's In-Depth state independently.

### Verified by code-reading — five user states
| State | Trigger | Next Action panel |
|-------|---------|--------------------|
| Anonymous | `!user` | "Take the Free Assessment" → `/assessment/start` |
| Free lead | `user.readiness` only | **"Go deeper with the In-Depth Assessment"** → `/assessment/in-depth $99` (primary) + Foundation (secondary) |
| In-Depth buyer | `user.readiness` + `assessments.inDepth.entitled` | "Enroll in AiBI-Foundation — $295" framed against their existing diagnosis; In-Depth card below shows Briefing access |
| Foundation buyer | `dashboard.enrollment` set | "Module N: title" + Continue Lesson + Practice for 5 Minutes |
| Both | enrollment + In-Depth | enrollment-progress Next Action + In-Depth card (View Briefing / Take assessment) |

### Cross-cutting items verified
- **Gmail alias entitlement** — `emailVariants()` is wired in `/courses/foundation/program/[module]` and `/assessment/in-depth/take` gates and in the dashboard `getEnrollment()` helper. Same logic across all three.
- **No free user sees paid content by accident** — every Foundation asset route now redirects non-enrolled users (post #94 + this fix on `/practice/[repId]`).
- **Locked artifact states** — dashboard `artifacts` array marks `moduleNumber > 1` as `locked` for users on module 1; status flips to `completed` when the source activity is done. Intentional.
- **Loading state** — dashboard renders `null` while `loading` is true (line 125). No flash of unauthed content.

### Verdict
**PASS.** Two real bugs found and fixed (one entitlement leak, one funnel ladder gap). Five user states verified by code reading.

---

## Issue #96 — Spec source-of-truth cleanup

### What landed
- **`Plans/aibi-launch-spec-v2.md`** — new active May 2026 launch spec. Ten sections: product ladder, naming, routes, entitlements, pricing, assessment logic, checkout/webhook, dashboard states, launch QA checklist, deferred items. Companion to `DECISIONS.md`. When code disagrees with it, file an issue.
- **`Plans/README.md`** — status index for the directory. Maps each archived HTML spec to the v2 section that supersedes it.
- **`CLAUDE.md` Reference Plans table** — rewritten. Points to v2 as the canonical source; demotes the seven HTML files to "Archive" with explicit superseded-by mapping. Fixed the "current version: v1" line that was stale (it's `v2` now).

### Stale assumptions explicitly buried
The v2 spec + Plans/README.md call out and supersede every stale assumption issue #96 listed:
- 8-question free assessment → **12 questions** (§1a, §6)
- 8–32 scoring range → **12–48 free, 48–192 In-Depth raw** (§1a, §1b, §6)
- `/foundations` as active product route → **legacy redirect** to `/education` (§3)
- `$97` Foundations product → **retired** before launch (§1c)
- Kajabi/future LMS language → **in-house LMS** (§4, §10)
- AiBI-P as public product name → **legacy internal slug only** (§2, §4)
- AiBI-Practitioner naming → **AiBI-Foundation** (§2)
- AiBI Foundations plural naming → **AiBI-Foundation singular** (§2)

### README cross-check
The repo `README.md` was rewritten as part of issue #88 (PR #90 commit `5284108`) to position the three-offer ladder + lifetime access. Cross-checks against v2 §1: aligned.

### Verdict
**PASS.** One current launch spec exists, indexed, and pointed at from CLAUDE.md and Plans/README.md. Eight stale assumptions explicitly buried.
